#include <stdio.h>
#include <string.h>

int main()
{
    char s1[10],s2[10],s3[10];
    printf("Enter two strings :");
    scanf("%s\n%s",s1,s2);
    strcat(s3, s1); 
    strcat(s3, s2); 

    printf("Strings after concatenation \n");
    printf("%s",s3);

    return 0;
}
