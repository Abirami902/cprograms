#include <stdio.h>

int main()
{
    int N,i=2;
    printf("Enter a number");
    scanf("%d",&N);
    if(N%i==0)
    {
        printf("EVEN NUMBER");
    }
    else 
    {
        printf("ODD NUMBER");
    }

    return 0;
}
