#include <stdio.h>

int main()
{
    int a,b,temp=0;
    printf("Enter two numbers :");
    scanf("%d%d",&a,&b);
    printf("BEFORE SWAP: %d %d\n",a, b);
    temp = a;
    a = b;
    b = temp;
    printf("AFTER SWAP:%d %d",a,b);
    return 0;
}
