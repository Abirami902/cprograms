#include <stdio.h>

int main()
{
	int rows;
	printf("Number of rows: ");
	scanf("%d", &rows);

	for (int i = 1; i <= rows; i++) {
		for (int j = 0; j <= i; j++) {
			printf("%c ",'A' +j );
		}
		printf("\n");
	}
	return 0;
}
